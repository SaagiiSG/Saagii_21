import { defineConfig } from 'vite'

// Relative base so the build works when served from the
// GitHub Pages subpath (https://SaagiiSG.github.io/Saagii_21/).
export default defineConfig({
  base: './',
})
